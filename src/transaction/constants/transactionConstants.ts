export enum TRANSACTION_TYPES {
  IN = 'IN',
  OUT = 'OUT',
  ADJUSTMENT = 'ADJUSTMENT',
  TRANSFER = 'TRANSFER',
}

export enum SortBy {
  DATE = 'date',
  AMOUNT = 'amount',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}
