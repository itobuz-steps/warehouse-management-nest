export enum FOLDER_PATH {
  uploads = 'uploads',
  product = 'product',
  user = 'user',
}

export enum FILE_FIELD {
  variantImage = 'variantImage',
  profileImage = 'profileImage',
}

export const FILE_COUNT = 8;
